## Executando o primeiro no 1º terminal (Escutando conexão) <br>
> `gcc tcp_server.c -o exec_server -O2` <br>

> `./exec_server` <br>

## Executando o segundo no 2º terminal (Recebendo conexão) <br>
> `gcc tcp_client.c -o exec_client -O2` <br>

> `./exec_client` <br>
